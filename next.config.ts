import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // External market APIs are called from server components / route handlers.
  // Nothing special needed here yet; kept for future image/header config.
};

export default nextConfig;
